(lambda () x)
(lambda (x y) x y z)
(define x
    (lambda (y) y))
(x 10101)


(define not
  (lambda (bool)
    (if bool #f #t)))

(define tofu
  (lambda (cond conseq alt)
    (let ((nconseq (not conseq))
          (nalt (not alt)))
      (if cond nconseq nalt))))

(tofu 23 #f #t)

((if #f 1 x) 123)
(define x
    (lambda (a b)
        (if a b a)))
(x 1 3)
(x #f 3)
